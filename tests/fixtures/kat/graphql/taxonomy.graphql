schema {
  query: Query
  mutation: Mutation
}

type Query {
  user(id: ID!): User
}

type Mutation {
  login(token: String!): LoginResult
}

type User {
  id: ID!
  name: String
}

type LoginResult {
  ok: Boolean!
}
